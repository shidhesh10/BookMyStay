<% layout("/layouts/boilerplate") %>

    <div class="container mt-3">

  <% if(allListings.length === 0) { %>
    
    <div class="alert alert-danger col-8 offset-2 mt-5 text-center" role="alert" style="border-radius: 20px; box-shadow: 0 4px 8px rgba(0,0,0,0.1);">
      <h4 class="alert-heading display-6"><i class="fa-solid fa-magnifying-glass-minus"></i> No Matches Found</h4>
      <p class="mt-3">We couldn't find any listings for that location. Try searching for a wider area like "India" or "United Kingdom".</p>
      <hr>
      <a href="/listings" class="btn btn-danger btn-lg mt-2">View All Listings</a>
    </div>

  <% } else { %>

    <div class="row row-cols-lg-3 row-cols-md-2 row-cols-sm-1 mt-3">
        <% for(let listing of allListings) {%>
        <a href="/listings/<%= listing._id %>" class="listing-link">

        <div class="card col listing-card">
            <img src="<%= listing.image.url %>" class="card-img-top" style="height: 20rem;" alt="Listing image">
            
            <div class="card-img-overlay"></div>
                    <div class="card-body">
                        <p class="card-text">
                            <b><%= listing.title %></b> <br>
                            <b>&#8377; 
                                <span class="listing-price" data-base-price="<%= listing.price %>">
                                <%= listing.price.toLocaleString("en-IN") %>
                                </span>
                            </b> / Night

                            <i class="tax-info"> &nbsp; (includes 18% GST)</i>
                        </p>
                    </div>
        </div>
        </a>
        <%}%>
    </div>

    <% } %> </div>
    
<script>
    let taxSwitch = document.getElementById("switchCheckDefault");

    if(taxSwitch) {
        taxSwitch.addEventListener("click", () => {
            let taxInfo = document.getElementsByClassName("tax-info");
            for (let info of taxInfo) {
                if (info.style.display != "inline") {
                    info.style.display = "inline";
                } else {
                    info.style.display = "none";
                }
            }

            let prices = document.getElementsByClassName("listing-price");
            for (let price of prices) {
                // Get the original price stored in the data attribute
                let basePrice = parseFloat(price.dataset.basePrice);
            
                if (taxSwitch.checked) {
                    // Calculate 18% tax
                    let taxValue = basePrice * 0.18;
                    let totalPrice = basePrice + taxValue;
                    
                    // Update the text with the new total (formatted with commas)
                    price.innerText = totalPrice.toLocaleString("en-IN");
                } else {
                    // Revert back to the base price
                    price.innerText = basePrice.toLocaleString("en-IN");
                }
            }
        });
    }
</script>